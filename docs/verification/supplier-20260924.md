# Verification: supplier purchases (2026-09-24)

Decision: [0014](../decisions/0014-supplier-purchases.md).

Compilation: live Editor `recompile`, no errors. Authoring: `run_script AgentScripts/BuildDevSite.cs` → "DevSite authored", then FishNet "Refresh Default Prefabs"; `DefaultPrefabObjects.asset` (reorder) and `EquipmentGhost.mat` (known drift) reverted, as documented in development.md. Two `run_tests` calls straight after a recompile failed with a transport "Network error" and never started (confirmed with `test_status`); each succeeded on retry.

| Run | Filter | Matched | Result | Artifact |
| --- | --- | --- | --- | --- |
| Live Editor `run_tests` editor, async (NUnit test-run id 2) | none (all EditMode) | 125 (Baseline 4, Goods 85, Session 36) | 125 passed | [editmode.xml](supplier-20260924/editmode.xml) |
| Live Editor `run_tests` playmode, async (NUnit test-run id 2), before the hint-text change | none (all PlayMode) | 14 | 14 passed | [playmode-run1.xml](supplier-20260924/playmode-run1.xml) |
| Live Editor `run_tests` playmode, async (NUnit test-run id 2), final code | none (all PlayMode) | 14 | 13 passed, 1 failed | [playmode-final.xml](supplier-20260924/playmode-final.xml) |
| Live Editor host in `DevSite` via `SessionRoot.Configure` with an isolated save and identity | n/a | n/a | two Buy clicks: $500.00 → $492.50, inventory 10 dough and 60 belts | [inventory-supplier.png](supplier-20260924/inventory-supplier.png) |

The failure in the final run is `HotbarKeysOverAStackAndDropsOnTheHotbarAssignSlots` ("The pointer is over the dough stack"), which failed on the unmodified baseline in earlier records and **passed** in run 1 above with no change to it or the code it drives. It is intermittent (hypothesis, unconfirmed: it depends on Game view size/focus because it maps panel coordinates to a virtual mouse), not a regression from this step.

New coverage:

- `PurchaseTests` (Goods, 5): offers must be valid content; a purchase debits the company and delivers a fresh lot to the buyer, committed together; a retried request is never charged twice, including after a reload; every rejection (`forbidden`, `invalid-offer`, `no-company`, `insufficient-funds`, `invalid-identity`, `capacity`) changes no cash or goods; a failed commit rolls back and the request can be retried.
- `SessionAuthoringTests` (+1, 1 changed): both offers are valid server content for items with definitions; `DevSite` lists them.
- `EquipmentPlacementTests.HostAndRemoteBuyFromTheSupplierWithCompanyCash` (PlayMode, new): the host buys through the supplier window's path and a remote UDP client with a raw request (and its retry); both packs arrive, the shared balance drops by exactly two packs, and the committed save agrees.

Not verified: a player build; a two-process session.
