# Verification: SQLite storage (2026-09-23)

Decision: [0011](../decisions/0011-sqlite-for-all-data-storage.md).

| Run | Filter | Matched | Result | Artifact |
| --- | --- | --- | --- | --- |
| `unity test . --mode EditMode` (batchmode, test-run id 2) | none (all EditMode) | 97 (Baseline 4, Goods 64, Session 29) | 97 passed | [editmode.xml](sqlite-20260923/editmode.xml) |
| Live Editor `run_tests` playmode, async | none (all PlayMode) | 12 | 11 passed, 1 failed | status only (Pipeline runner, no XML) |
| Live Editor `run_tests` playmode, baseline (changes stashed) | testName `...EquipmentPlacementTests.HotbarKeysOverAStackAndDropsOnTheHotbarAssignSlots` | 1 | 1 failed, same message | status only |

The PlayMode failure (`The pointer is over the dough stack. Expected "dough" but was null`) also fails on the pre-change baseline, so it is not caused by this change. The test passes the storage-dependent step (the dough stack appears in the inventory) and fails at mouse-position-to-slot hover mapping; the hypothesis is a Game view size dependence, which is unconfirmed.

New/changed coverage: legacy snapshot dry run and import (v1, v2, v3 payloads), refusal to import over an existing database, corrupt latest row recovery with quarantine, SQLite client identity and legacy `client.secret` import, one-time legacy world import through `DevWorld.LoadOrCreate`. All test saves use unique temp directories.

Not verified: a player build, and a two-process session on the new storage.
