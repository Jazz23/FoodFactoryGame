# Controls and working oven verification — 2026-09-22

Branch `placement`. Editor: Unity 6000.5.9f1, project `E:\Projects\Unity\FoodFactoryGame`, driven through Unity CLI MCP. Decision: [0007](../decisions/0007-player-controls-and-working-oven.md). The Pipeline returns no native run ID, so runs are identified by the NUnit `test-run` start time in the XML Unity writes; each XML was copied right after its run.

## Final runs (code as committed)

| Run identity | Requested filter / mode | Matched | Passed | Failed | Skipped | Artifact |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| NUnit test-run id 2, start 2026-09-23 05:28:54Z | `FoodFactoryGame` (test name) / editor (async) | 69 | 69 | 0 | 0 | [`artifacts/oven-20260922-editmode.xml`](artifacts/oven-20260922-editmode.xml) |
| NUnit test-run id 2, start 2026-09-23 05:28:25Z | `FoodFactoryGame.Session.PlayModeTests` (assembly) / playmode (async) | 5 | 5 | 0 | 0 | [`artifacts/oven-20260922-session-playmode.xml`](artifacts/oven-20260922-session-playmode.xml) |
| response only (XML overwritten by the later run) | `FoodFactoryGame.Goods.PlayModeTests` (assembly) / playmode (async) | 1 | 1 | 0 | 0 | — |

EditMode: Goods 41 (one new `EquipmentTests.StarterGoodsArriveOnlyWithANewInventory`), Session 24 (new `BreadRecipeTurnsDoughIntoBreadInTheOven`; the DevSite, player-prefab and input-map tests updated for the HUD, the removed `Orbit` action and the new actions), Baseline 4. The Goods PlayMode run preceded the last presentation-only edits (shoulder offset, aim raycast, hotbar height, empty-reason guard); the bridge was unchanged after it.

`recompile`: `compilationFailed: false`; the only compiler warnings are the two existing FishNet `CS0618` warnings. `BuildDevSite` and asset imports log FishNet's `… same assetPath hash of 0` error until *Refresh Default Prefabs* is run ([development](../development.md#session-bootstrap-host-join-multi-process)).

## Coverage

- Domain: starter goods land in a new inventory with stable IDs, owned by the site, committed; a rejoin after they were used grants nothing; a failed commit and an oversized starter set leave the world byte-identical.
- Authoring: `DevSite` has one `PlayerHud` (document with panel settings, interaction) and `EquipmentInteraction` has all eight action references; `SessionRoot.recipes` lists `Bread.asset`; the recipe is `oven`, 1 dough → 1 bread, 10 s and registers in a `GoodsWorld`; the Player map has no `Orbit`, and has `Remove` (right mouse), `Inventory` (E), `ClearCursor` (Q), `CloseScreen` (Esc), all presses, and `Hotbar` with `<Keyboard>/1..9` scaled to 1..9.
- PlayMode `HostBakesBreadThroughTheOvenScreenAndRemoteSeesItRun` (real `DevSite`, host plus loopback-UDP remote, temp paths): storage holds 20 dough and each player 5. The remote's start gets `missing-inputs` on an empty oven and `invalid-recipe` for an unknown recipe. The host opens the oven screen (recipe, Start and inventory-dough buttons present) and moves its dough in through `EquipmentInteraction.MoveGoods` (the stack button's handler); the input-stack button appears. The host's Start is accepted: the remote sees the job started by the host, the host's oven visual is running, and exactly one dough was consumed. The remote's second start gets `station-busy`. One bread appears in the output, the visual goes idle, and no further job starts. Moving the output puts the bread in the host's inventory, and the committed save has it.
- The existing placement, restart and session-bootstrap PlayMode tests still pass with the new controls.

## Running-game captures (Editor play mode, host, isolated temp save)

Captured with `capture_game_view` (`source: screen`) after hosting through `SessionRoot.Configure` + `Begin(Host)` with a temp save directory. Input was driven by calling the handlers (`SelectSlot`, the private `OnPlace` click handler, and UI Toolkit submit events on the HUD buttons), not by real mouse and keyboard events.

| File | Shows |
| --- | --- |
| [`01-crosshair-on-oven.png`](oven-20260922/01-crosshair-on-oven.png) | Locked-pointer crosshair beside the avatar (over-the-shoulder pivot), hotbar slot 1 "Oven". |
| [`02-inventory-screen.png`](oven-20260922/02-inventory-screen.png) | E screen: Inventory 5/10 "Dough ×5", Machines "none", Storage 20/100 "Dough ×20". |
| [`03-hotbar-cursor-ghost.png`](oven-20260922/03-hotbar-cursor-ghost.png) | After pickup and slot 1: slot highlighted "Oven ×1", green footprint at the crosshair. |
| [`04-oven-screen-baking.png`](oven-20260922/04-oven-screen-baking.png) | Oven opened through the left-click path: recipe selected, Start disabled, progress "Making Bread: 3/10 s", Input 4/10. |
| [`05-oven-running-glow.png`](oven-20260922/05-oven-running-glow.png) | Oven front with the heater glowing during a batch (presenter `Running` = true, `OvenToggle.IsPowered` = true). |
| [`06-bread-collected.png`](oven-20260922/06-bread-collected.png) | After clicking the output stack: Inventory 4/10 "Bread ×4", Output 0/4, Start enabled, "Idle". |

During the capture session, placement via `OnPlace` cleared the cursor once the last oven was placed, and a crosshair ray that met the floor just in front of the oven's legs found no machine (lowering the pitch made it hit, and the click opened the screen). Editing scripts during play mode made Unity recompile and reload, which dropped the live session and turned a null status string into an empty one; that was an Editor workflow effect, not a runtime path, and is now noted in [development](../development.md).

## Not verified

- Real mouse/keyboard input: pointer lock, `Look` orbiting, the hotbar keys and E/Q/Esc/right click were not pressed physically or synthesised; their bindings are covered by the authoring test and their handlers by the captures.
- A Windows player build and a separate-process remote client with these controls.
- Independent review of the visual and control changes.
